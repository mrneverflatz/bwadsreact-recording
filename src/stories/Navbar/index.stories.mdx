import { Meta, Canvas, Story, Source } from "@storybook/addon-docs";
import dedent from "ts-dedent";

import "assets/css/index.css";

import Navbar from "./example/Sample";

<Meta title="Components/Navbar" />

# Navbar

A most import component that simply let user interact with the app, [[Default Example](/docs/components-navbar-example--default)] if you want to see in action

export const Template = (args) => <Navbar {...args} />;

<Story name="Introduction" args={{}}>
  {Template.bind({})}
</Story>
