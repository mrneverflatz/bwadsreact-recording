import { Meta, Canvas, Story, Source } from "@storybook/addon-docs";
import dedent from "ts-dedent";

import "assets/css/index.css";

import Button from "./example/Sample";

<Meta title="Components/Button" />

# Button

A most import component that simply let user interact with the app, [[Default Example](/docs/components-button-example--success)] if you want to see in action

export const Template = (args) => <Button {...args} />;

<Story name="Introduction" args={{}}>
  {Template.bind({})}
</Story>
