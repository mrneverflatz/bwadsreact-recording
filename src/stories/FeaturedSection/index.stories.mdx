import { Meta, Canvas, Story, Source } from "@storybook/addon-docs";
import dedent from "ts-dedent";

import "assets/css/index.css";

import FeaturedSection from "./example/Sample";

<Meta title="Components/FeaturedSection" />

# FeaturedSection

A most import component that simply let user interact with the app, [[Default Example](/docs/components-featured-section-example--success)] if you want to see in action

export const Template = (args) => <FeaturedSection {...args} />;

<Story name="Introduction" args={{}}>
  {Template.bind({})}
</Story>
